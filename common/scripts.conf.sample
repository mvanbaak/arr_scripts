# SAMPLE configuration file - shared settings (app-independent).
# Move this file to 'scripts.conf' in the same directory (common/).
# Sourced by every script BEFORE the per-app config
# (radarr/scripts.conf or sonarr/scripts.conf), so per-app values override.

# TMDB API key for trailer, recap, and release-date lookups
# Get one at https://www.themoviedb.org/settings/api
TMDB_API_KEY="your_tmdb_api_key"

# TMDB Website credentials (for tmdb_login.sh).
# Required for push_physical_to_tmdb.sh to authenticate with TMDB website.
TMDB_USERNAME=""
TMDB_PASSWORD=""

# Path to Netscape-format cookie file for TMDB website authentication.
# Used by push_physical_to_tmdb.sh. Default: ~/.tmdb_cookies.txt
# TMDB_COOKIE_FILE=""

# Path to a Netscape-format cookie file for yt-dlp YouTube authentication.
# Optional - leave empty to download without authentication.
# See docs/cookie-extraction.md for instructions on exporting cookies from your browser.
YT_DLP_COOKIE_FILE=""

# Autopulse notification (optional). Uncomment and set URL to enable Plex scan after trailer download.
# AUTOPULSE_URL="http://ip:3030"
# AUTOPULSE_TRIGGER="manual"
# AUTOPULSE_AUTH_USER="username"
# AUTOPULSE_AUTH_PASS="password"

# yt-dlp format selector. Default picks the best H.264 (avc1) video at 1080p or
# below plus AAC audio, merged to MP4 without re-encoding. This avoids pulling
# 4K/AV1 sources and transcoding them on older hardware.
# YT_DLP_FORMAT="bv*[height<=1080][vcodec^=avc1]+ba[acodec^=mp4a]/b[height<=1080][vcodec^=avc1]"

# Dry-run mode. When true, skips actual changes, shows what would happen.
# Override per-invocation with --dry-run flag.
DRY_RUN="false"

# Debug mode. When true, prints detailed diagnostic messages.
# Override per-invocation with --debug flag.
DEBUG="false"